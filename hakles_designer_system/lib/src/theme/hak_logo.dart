import 'package:flutter/material.dart';

class HakLogo extends StatelessWidget {
  const HakLogo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: 100,
      color: Colors.red,
    );
  }
}
